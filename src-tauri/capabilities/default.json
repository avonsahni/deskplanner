{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the planner's three windows. Without core:event the frontend's listen() is denied by the ACL, and no window ever hears that the data changed.",
  "windows": ["wallpaper", "planner", "launcher"],
  "permissions": ["core:default"]
}
